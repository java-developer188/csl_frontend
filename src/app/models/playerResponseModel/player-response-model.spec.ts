import { PlayerResponseModel } from './player-response-model';

describe('PlayerResponseModel', () => {
  it('should create an instance', () => {
    expect(new PlayerResponseModel()).toBeTruthy();
  });
});
