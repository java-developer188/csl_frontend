import { TeamResponseModel } from './team-response-model';

describe('TeamResponseModel', () => {
  it('should create an instance', () => {
    expect(new TeamResponseModel()).toBeTruthy();
  });
});
